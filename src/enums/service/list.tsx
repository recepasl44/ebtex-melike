export enum ServicesListStatus {
  IDLE = "IDLE",
  LOADING = "LOADING",
  SUCCEEDED = "SUCCEEDED",
  FAILED = "FAILED",
}

export enum IServiceEnum {
  "CLOSE" = 0,
  "OPEN" = 1,
}
export default ServicesListStatus;
