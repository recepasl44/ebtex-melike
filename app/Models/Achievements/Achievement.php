<?php

namespace App\Models\Achievements;

use App\Models\ModelTrait;
use Illuminate\Database\Eloquent\Model;
use App\Models\Achievements\Traits\AchievementAttribute;
use App\Models\Achievements\Traits\AchievementRelationship;

class Achievement extends Model
{
    use ModelTrait,
        AchievementAttribute,
    	AchievementRelationship {
            // AchievementAttribute::getEditButtonAttribute insteadof ModelTrait;
        }

    /**
     * NOTE : If you want to implement Soft Deletes in this model,
     * then follow the steps here : https://laravel.com/docs/6.x/eloquent#soft-deleting
     */

    /**
     * The database table used by the model.
     * @var string
     */
    protected $table = 'achievements';

    /**
     * Mass Assignable fields of model
     * @var array
     */
    protected $fillable = [

    ];

    /**
     * Default values for model fields
     * @var array
     */
    protected $attributes = [

    ];

    /**
     * Dates
     * @var array
     */
    protected $dates = [
        'created_at',
        'updated_at'
    ];

    /**
     * Guarded fields of model
     * @var array
     */
    protected $guarded = [
        'id'
    ];

    /**
     * Constructor of Model
     * @param array $attributes
     */
    public function __construct(array $attributes = [])
    {
        parent::__construct($attributes);
    }
}
